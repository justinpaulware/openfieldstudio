# Roadmap

## In progress — verification pass (backend back online)
- [x] Add `geometry_type` column to comments + trigger update; export reads the column
- [ ] Create throwaway test project with sample point/line/polygon layers
- [ ] Verify label backgrounds + text case/opacity controls (Tab 16 phase 1)
- [ ] Verify Positron (no labels) basemap in editor and published viewer
- [ ] Verify comment export (CSV + GeoJSON, filters, empty case)
- [ ] Verify comments end-to-end on a published map (approval on/off)
- [ ] Verify ArcGIS REST service expansion (FeatureServer, MapServer, service root, paging, errors)
- [ ] Verify Publish tab views cleanup (header, per-row controls, Main sync)
- [ ] Verify layer filters + per-view overrides persist
- [ ] Delete the test project

## Next
- [ ] Tab 14 — Engagement: line & polygon feedback, owner-enabled per project
