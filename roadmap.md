# Roadmap

## Verification pass — complete (2026-09-03, live browser check)
- [x] Throwaway test project with point/line/polygon layers (created, verified, deleted)
- [x] Label backgrounds + weight/text case/opacity controls (Tab 16 phase 1)
- [x] Positron (no labels) basemap in editor and published viewer
- [x] Comment export (CSV with contact details + GeoJSON)
- [x] Comments end-to-end on a published map (immediate, no approval)
- [x] ArcGIS REST expansion (MapServer service root → layer picker → 51 features)
- [x] Publish tab views cleanup (clean header, per-row Open/Publish/Unpublish, Main sync)
- [x] Layer filters persist across reload

## Known minor issue
- [ ] Breadcrumb status badge still reads "Draft" immediately after publishing (corrects on reload) — project query not invalidated by the publish mutation


## Next
- [ ] Tab 14 — Engagement: line & polygon feedback, owner-enabled per project
